fn gcd(a: Int, b: Int) : Int {
    if b == 0 {
        return a;
    }
    return gcd(b, a % b);
}

fn execute_math() : [] {
    let out: Int = gcd(1071, 462);
    print out; // Should print 21
    
    // Satisfying the strict unit return requirement
    return (); 
}

execute_math();